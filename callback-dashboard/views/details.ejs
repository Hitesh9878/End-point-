<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Call Details</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <h1>Call Details</h1>
    <a href="/">&larr; Back to Dashboard</a>
    
    <% if (record) { %>
      <div class="detail-card">
        <div class="field">
          <label>CallSid:</label>
          <span><%= record.id %></span>
        </div>
        <div class="field">
          <label>Status:</label>
          <span class="status-<%= record.status %>"><%= record.status %></span>
        </div>
        <div class="field">
          <label>Recording:</label>
          <span>
            <% if (record.recording !== 'N/A') { %>
              <a href="<%= record.recording %>" target="_blank">Listen</a>
            <% } else { %>
              N/A
            <% } %>
          </span>
        </div>
        <div class="field">
          <label>Updated At:</label>
          <span><%= record.timestamp %></span>
        </div>
        <div class="field">
          <label>Received At:</label>
          <span><%= record.received %></span>
        </div>
      </div>
    <% } else { %>
      <p>Record not found</p>
    <% } %>
  </div>
</body>
</html>